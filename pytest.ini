[pytest]
addopts = -ra --strict-markers --junitxml=reports/junit.xml
markers =
    scip: tests requiring pyscipopt / SCIP backend
    gurobi: tests requiring gurobipy / Gurobi backend
    glpk: tests requiring swiglpk / GLPK backend
    highs: tests requiring highspy / HiGHS backend
    data: tests requiring data extras (pandas, polars, SQLAlchemy)
    bridge: tests requiring bridge extras (pyomo)
